# Recording Rules
```bash
# add recording-rules
vi recording_rules.yml

# add alertmanager server
vi docker-compose.yml

# start all servers
docker-compose up -d

# check alert status
open http://localhost:9090/


docker-compose down
```